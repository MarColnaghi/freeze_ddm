function model = model_exp1()
% Explicit spec with one block per model component

model.lambda = struct( ...
    'predictors', {{ ...
    struct('name', 'sm', 'bounds', [-1, 1]),...
    struct('name', 'fs', 'bounds', [-1, 1]),...
    struct('name', 'ln', 'bounds', [-1, 1]),...
    struct('name', 'ls', 'bounds', [-1, 1]),...
    struct('name', 'intercept', 'bounds', [0, 4])
    }}, ...
    'link', @(x) x, ...
    'ground_truth', [0 0 0 0 0] ...
    );

